{
  "name": "okusuri-monorepo",
  "private": true,
  "scripts": {
    "setup": "mise run setup",
    "dev:backend": "mise run backend_dev",
    "dev:frontend": "mise run frontend_dev",
    "dev": "mise run dev",
    "test": "mise run test"
  }
}
